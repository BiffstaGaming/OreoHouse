services:
  oreohouse:
    build: .
    image: oreohouse:dev
    container_name: oreohouse
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      OREOHOUSE_ADDR: ":8080"
      OREOHOUSE_DATA_DIR: "/data"
    restart: unless-stopped
